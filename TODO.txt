all image functions
documentation
editor
structs struct name start number x number y string s end
    when getting name that's _alpha123 if it has dots resolve it
cfg_save success
maps instead of vectors for variables/etc
bytecode? optimize?
call function declared ahead (preload funcs)
bus wheels rotate with two joys
two player twin stick circles fire at each other start green -> yellow -> red
loop sfx
nibbles
optimize functions calls - no dup just reset p/line/etc
